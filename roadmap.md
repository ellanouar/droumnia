
## Header + hero fidelity (Sep 4)
- Header must match the reference image exactly (transparent, logo left, centered nav, olive pill button right).
- Hero photo must fade/blend into the background exactly like the reference (soft left/bottom fade, full-bleed right).
- Keep and extend the 3D animations.
